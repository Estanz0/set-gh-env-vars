on: [push]

jobs:
    test_action:
        runs-on: ubuntu-latest
        name: Test this action
        steps:
            - name: Set Repository Variable
              uses: Estanz0/set-gh-env-vars@v1.3
              with:
                  name: 'Test v1.3'
                  value: 'Test v1.3'
